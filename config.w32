// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("xcb", "for xcb support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("xcb", "enable xcb support", "no");

if (PHP_XCB != "no") {
	EXTENSION("xcb", "xcb.c");
}

